  <div class="green-banner">
    <h1><i>"De beste interviews met campinghouders in de BeNeLux"</i></h1>
    <p><i>Een simpele website zonder poespas, maar mét <b>echte</b> verhalen over <b>echte</b> mensen.</i></p>
</div>
<div class="general-container">
  <div class="interview">

<h2>Sign up</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :first_name,
                label: "Voornaam",
                required: true,
                autofocus: true,
                input_html: { autocomplete: "Voornaam" }%>
    <%= f.input :last_name,
                label: "Achternaam",
                required: true,
                autofocus: true,
                input_html: { autocomplete: "Achternaam" }%>
    <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" }%>
    <%= f.input :password,
                label: "Wachtwoord",
                required: true,
                hint: ("Minimaal #{@minimum_password_length} karakters " if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation,
                label: "Bevestiging wachtwoord",
                required: true,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :picture,
                label: "Upload uw profiel foto" %>
    <%= f.input :picture_cache, as: :hidden %>

  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up", class: "btn camping-btn" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>
