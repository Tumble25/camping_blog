  <div class="green-banner">
    <h1><i>"De beste interviews met campinghouders in de BeNeLux"</i></h1>
    <p><i>Een simpele website zonder poespas, maar mét <b>echte</b> verhalen over <b>echte</b> mensen.</i></p>
</div>
<div class="general-container">
  <div class="interview">
<h2>Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "Huidig wachtwoord" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in", class: "btn camping-btn" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>
